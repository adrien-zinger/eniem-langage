let not_equal = (left, right) {
	i32_not_equal!(left, right)
}

let not0 = (var) {
	var != 0
}

let maybe_zero = 10

let div = (var: truc) {}

/*
let div = (num: number, den: not0) {
	if verified is not0 {
		/* ok */
	} else {
		panic!("fsdqfqs")
	}
}
*/

i32_div!(a, b)

